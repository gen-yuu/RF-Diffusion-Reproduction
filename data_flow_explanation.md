# RF-Diffusion (WiFi) のデータフローと評価指標

## データの実体と変換フロー

### WiFi CSI（Channel State Information）とは
- **実体**: 複素数の時系列データ（電波の伝搬特性）
- **形式**: (時間ステップ数, サブキャリア数) の2次元配列
- **例**: (1545, 90) の複素数配列 → 各要素が実部+虚部を持つ
- **物理的意味**: WiFi送受信機間の無線チャネルの状態

### 1サンプルのデータ構成

```
.mat ファイル (例: user1-1-1-5-11-r5.mat)
├── cond: (1, 6)           ← 入力条件（環境パラメータ）
│   └── [1, 1, 1, 5, 5, 1]  (ユーザーID, 位置, 向き等)
│
└── feature: (1545, 90)    ← 正解のWiFi CSI（複素数データ）
    └── 4.04+2.88j, 5.19-2.88j, ...
```

## モデルの入出力

### 入力 (Input)
```
cond: 環境条件パラメータ (1, 6)
  ↓
[モデルが学習すべき情報]
「この環境条件では、WiFi信号はこのように伝搬する」
```

### 出力 (Output)  
```
predicted CSI: 予測されたWiFi CSI (1545, 90) 複素数
  ↓
[モデルが生成したもの]
「条件から推定した信号の伝搬パターン」
```

### 正解データ (Ground Truth)
```
feature: 実測されたWiFi CSI (1545, 90) 複素数
  ↓
[実際の測定値]
「この環境で実際に観測された信号」
```

## 評価指標の計算フロー

### SSIM（Structural Similarity Index）の場合

```
入力: cond (条件)
  ↓
┌─────────────────────┐
│   Diffusion Model   │ ← 学習済みモデル
└─────────────────────┘
  ↓
predicted CSI (複素数データ, 1545×90)
  ↓
  ├─ 複素数のまま比較 ─┐
  ↓                    ↓
Ground Truth CSI    Predicted CSI
 (1545×90)          (1545×90)
  └─────┬─────────────┘
        ↓
   eval_ssim() 関数で直接比較
        ↓
   **1つのSSIM値** (例: 0.8061)
```

**重要**: SSIMはCSIデータ（複素数）を**そのまま**比較
- スペクトログラムには変換**しない**
- 時系列データとして直接評価

### FID（Fréchet Inception Distance）の場合

```
入力: cond (条件)
  ↓
┌─────────────────────┐
│   Diffusion Model   │
└─────────────────────┘
  ↓
predicted CSI (複素数データ, 1545×90)
  ↓
  ├─ 画像変換が必要 ──┐
  ↓                   ↓
Ground Truth CSI   Predicted CSI
  ↓                   ↓
[STFT変換]         [STFT変換]
  ↓                   ↓
スペクトログラム    スペクトログラム
(周波数×時間)      (周波数×時間)
  ↓                   ↓
[dB変換+画像化]    [dB変換+画像化]
  ↓                   ↓
画像 (.jpg)        画像 (.jpg)
  └────┬──────────────┘
       ↓
  多数の画像を集める
       ↓
  Inception-v3で特徴抽出
       ↓
  分布間の距離を計算
       ↓
  **1つのFID値** (スカラー)
```

### スペクトログラム変換の詳細（FIDのみ）

```python
# inference.py の save_wifi 関数より

# 1. CSI複素数データを1次元化
data = data[0, :, 0].reshape(512)  # 複素数512サンプル

# 2. STFT（短時間フーリエ変換）でスペクトログラム化
data_spec = torch.stft(data, n_fft=24, hop_length=17)
#           ↑ 時系列 → 時間-周波数表現

# 3. 振幅スペクトログラムに変換
data_spec_mag = torch.abs(data_spec)

# 4. dBスケールに変換
data_spec_dB = 20 * np.log10(data_spec_mag + 1e-6)

# 5. 画像として保存
plt.imshow(data_spec_dB, cmap='viridis')
plt.savefig('spectrogram.jpg')
```

## まとめ

### WiFi CSI（正解データ）とは
- **実体**: 複素数の時系列データ（測定された電波の状態）
- **役割**: モデルが予測すべき目標値

### スペクトログラムとは
- **実体**: CSIデータをSTFT変換した画像（時間-周波数表現）
- **役割**: FID計算専用（画像ベースの評価指標のため必要）

### データ変換の有無

| 評価指標 | 入力データ | 評価対象 | 変換 |
|---------|---------|---------|------|
| **SSIM** | CSI複素数 | CSI複素数 | **なし**（そのまま比較） |
| **FID**  | CSI複素数 | スペクトログラム画像 | **あり**（STFT→画像変換） |

### なぜ2つの評価方法？

1. **SSIM**: 信号の構造的類似度を直接評価（信号処理的観点）
2. **FID**: 視覚的・知覚的な品質を評価（画像としての品質）

どちらも重要で、異なる側面から生成品質を評価しています。
